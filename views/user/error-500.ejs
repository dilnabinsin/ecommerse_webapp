<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error 500 - TK Saree Studio</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Favicon -->
    <link rel="shortcut icon" href="/asserts/images/favicon.png">
    <style>
        :root {
            --primary: #1a2526; /* Deep charcoal */
            --accent: #b7950b; /* Gold */
            --text-light: #f5f5f5; /* Off-white */
            --bg-gradient: linear-gradient(135deg, #1a2526 0%, #3b4f50 100%);
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: var(--bg-gradient);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-light);
        }

        .error-container {
            text-align: center;
            padding: 20px;
            max-width: 600px;
            margin: 0 auto;
            animation: fadeIn 1s ease-in-out;
        }

        .error-code {
            font-size: 8rem;
            font-weight: 700;
            color: var(--accent);
            text-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            animation: pulse 2s infinite;
        }

        .error-title {
            font-size: 2rem;
            font-weight: 600;
            margin: 10px 0;
        }

        .error-message {
            font-size: 1.2rem;
            font-weight: 300;
            opacity: 0.9;
            margin-bottom: 30px;
        }

        .back-home-btn {
            display: inline-flex;
            align-items: center;
            padding: 12px 30px;
            font-size: 1rem;
            font-weight: 500;
            color: var(--primary);
            background: var(--accent);
            border: none;
            border-radius: 50px;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .back-home-btn i {
            margin-right: 8px;
        }

        .back-home-btn:hover {
            background: #d4a012;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

        .footer-text {
            margin-top: 40px;
            font-size: 0.9rem;
            font-weight: 300;
            opacity: 0.7;
        }

        /* Animations */
        @keyframes fadeIn {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        /* Responsive Design */
        @media (max-width: 576px) {
            .error-code {
                font-size: 5rem;
            }

            .error-title {
                font-size: 1.5rem;
            }

            .error-message {
                font-size: 1rem;
            }

            .back-home-btn {
                padding: 10px 20px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-code">500</div>
        <h2 class="error-title">Oops! Something Went Wrong</h2>
        <p class="error-message">We're experiencing an internal server error. Our team is working to fix it. Please try again later.</p>
        <a href="<%= link %>" class="back-home-btn"><i class="fas fa-home"></i> Back to Home</a>
        <p class="footer-text">Copyright &copy; 2023 TK Saree Studio. All rights reserved.</p>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>